# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# Local config (API keys — never commit)
config_local.py
.env

# Saved browser session (contains auth cookies — never commit)
data/session.json

# Databases (large binary files — generated locally)
data/db/*.db
data/db/*.db-wal
data/db/*.db-shm

# Static site data artifact is intentionally versioned for GitHub Pages.

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local settings
.claude/
CLAUDE.md

# Debug screenshots (local only)
debug_*.png
*.png
